logging:
  level:
    root: INFO
spring:
  datasource:
    url: jdbc:mysql://challenge-mysql:3306/ciclic?characterEncoding=UTF-8
    username: root
    password: mysql
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: create-drop
